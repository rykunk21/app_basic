# REQ-001: Workspace compiles clean with all three crate members

## Acceptance Criteria
`cargo build --workspace` exits 0. All three members compile: backend,
frontend (targeting wasm32), gates. No warnings treated as errors yet.

## Symbols
- `Cargo.toml` — workspace members list
- `gates::main` — gates crate entry point
- `gates/Cargo.toml` — gates crate manifest

## Test
`test_gates_binary_exits_zero`
Location: `gates/tests/integration/smoke_test.rs`

## Linked Issue
https://github.com/rykunk21/app_basic/issues/3

## Milestone
M1: Project Infrastructure

